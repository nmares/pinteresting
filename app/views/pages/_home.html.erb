<div class="jumbotron center">
	<h1>Welcome!</h1>
	<% if user_signed_in? %>
	<h3>To the members area</h3>
	<% else %>
		<p>
			<%= link_to "Log In", new_user_session_path, class: "btn btn-default btn-lg" %>
			<%= link_to "Sign Up", new_user_registration_path, class: "btn btn-danger btn-lg" %>
		</p>
	<% end %>
</div>
